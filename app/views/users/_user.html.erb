<li>
  <%= gravatar_for user, 70 %>
  <%= link_to user.name, user_path(user) %>
  <% if current_user.admin? && !current_user?(user) %>
    <%= link_to "delete", user, method: :delete,
            data: {confirm: "Delete profile of #{user.name}"}%>
  <% end %>
</li>